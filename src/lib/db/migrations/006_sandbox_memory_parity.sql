-- Migration 006: Sandbox durable memory parity
-- SUPERSEDED: All sandbox_memory_* tables are now created in 002_workspaces
-- as part of the canonical schema.
-- Kept as no-op to preserve _migrations tracking.
SELECT 1;
