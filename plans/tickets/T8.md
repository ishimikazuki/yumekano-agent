## 1. Ticket ID

`T8`

## 2. Goal

Add deterministic pre-ranker gates, run the full eval suite, and produce the final rollout recommendation.

## 3. Why this ticket exists

Candidate selection must stop unsafe, phase-breaking, or state-contradicting responses before model-based ranking, and the migration needs a measurable final eval before rollout.

## 4. In scope

- Add deterministic pre-ranker gates for hard safety violations, phase violations, contradictions with current CoE state, and memory contradictions.
- Preserve model-based ranking after the deterministic gates.
- Add tie-break and score explanation data to the trace.
- Run the full emotion and relationship eval suite.
- Produce a markdown report with pass and fail counts, weak cases, rollout recommendation, and feature-flag recommendation.
- Limit tuning in this ticket to configuration, thresholds, and weights.

## 5. Out of scope

- Architecture redesign for CoE, integrator, workflows, or persistence.
- Prompt source-of-truth work.
- Publish and versioning flow work.
- Legacy heuristic deletion.
- Later tickets that MUST NOT start yet: `T9`.

## 6. Required code areas

- `src/mastra/agents/ranker.ts`
- `src/mastra/scorers/*`
- Eval suite and eval report paths
- Trace explanation paths
- Ranker and eval tests under `tests/`

## 7. Forbidden code areas

- Core CoE extractor and integrator architecture files except for read-only contract use
- Workflow redesign files
- Publish and release flow files
- Legacy heuristic cleanup files

## 8. Acceptance criteria

- Unsafe candidates are removed before model-based ranking.
- Phase contradictions are removed before model-based ranking.
- Memory contradictions are removed before model-based ranking.
- The final eval report is generated.
- A rollout recommendation is documented.

## 9. Required tests

- Deterministic gate tests for safety violations
- Deterministic gate tests for phase violations
- Deterministic gate tests for CoE-state contradictions
- Deterministic gate tests for memory contradictions
- Trace tests for tie-break and score explanation output
- Full emotion and relationship eval suite run

## 10. Stop condition

- Stop if meeting acceptance requires a new architecture change rather than config or threshold tuning.
- Stop if the eval can only pass with live-only external calls.
- Stop if the work starts to delete legacy code; that belongs to `T9`.

## 11. Risks / notes

- The master plan does not fix one report output path; keep the report in the existing eval report area.
- "Contradiction with current CoE state" depends on the `T4` trace and state contract; if that contract is still unclear, stop instead of redefining it here.

## 12. Handoff prompt template

```text
Work only on T8 using plans/tickets/T8.md and COE_PAD_REPAIR_PLAN.md.
Add deterministic pre-ranker gates with failing tests first, tune only config and thresholds, run the full eval suite, and stop if the work starts redesigning architecture or deleting legacy code.
```
