## 1. Ticket ID

`T9`

## 2. Goal

Remove the old heuristic path only after the new path is fully verified.

## 3. Why this ticket exists

Legacy deletion is safe only after the new path is proven green, shadow comparison exists, and rollback instructions are documented.

## 4. In scope

- Confirm the prerequisite that `T0` through `T8` are green and that a shadow comparison report exists.
- Document the rollback plan before deletion.
- Preserve feature-flag rollback instructions.
- Remove the old heuristic path and unused adapters.
- Update related documentation so it matches the actual runtime path.

## 5. Out of scope

- Any new behavior work, tuning, or architecture redesign.
- Any additional rollout experimentation beyond documenting rollback.
- No later ticket exists; do not bundle unrelated cleanup or follow-up work into `T9`.

## 6. Required code areas

- Legacy heuristic appraisal path files
- Unused adapter files that only exist for the old path
- Feature-flag documentation or config references
- Documentation files that describe the active runtime path
- Full test suite entry points

## 7. Forbidden code areas

- New feature implementation in production or sandbox workflows
- Prompt or versioning redesign files
- Ranker or eval tuning files beyond keeping tests green

## 8. Acceptance criteria

- The old path is removed.
- Documentation matches the current runtime.
- The full test suite stays green.

## 9. Required tests

- Full unit test run
- Full integration test run
- Full migration test run
- Full relevant eval run

## 10. Stop condition

- Stop immediately if `T0` through `T8` are not all green.
- Stop immediately if the shadow comparison report does not exist.
- Stop immediately if rollback steps are not documented before deletion.

## 11. Risks / notes

- The master plan says "related documents" but does not enumerate them; update only documents that still describe the old heuristic path or the old flag behavior.
- Keep rollback instructions concrete even though the code path is being deleted.

## 12. Handoff prompt template

```text
Work only on T9 using plans/tickets/T9.md and COE_PAD_REPAIR_PLAN.md.
Do not start unless T0-T8 are green and a shadow comparison report exists, document rollback first, remove only the legacy heuristic path and unused adapters, and keep the full suite green.
```
