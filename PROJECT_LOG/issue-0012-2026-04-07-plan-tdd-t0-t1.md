# plan_tdd.md T0-T1 実施

## Goal
- plan_tdd.md（一新された TDD チケットプラン）の T0, T1 を実施する

## Done

### T0: テストゲートの意味論修正
- `test:unit` を `npm test` エイリアスから `tests/contracts/ + tests/unit/` のみに修正
- `test:emotion-regression` を空の `tests/regression/` から `tests/evals/emotion/*.test.ts` に修正
- `ci:local` に `test:emotion-regression` を追加
- `docs/TEST_SCRIPTS.md` を新規作成（scripts の役割ドキュメント）
- `tests/contracts/test-gate-contract.test.ts` 追加（22 assertions）
- `tests/contracts/ci-local-coverage.contract.test.ts` 追加（11 assertions）

### T1: migration 二重定義の解消
- `MIGRATION_004` を no-op 化（generator_intimacy_md は 001/002 に canonical）
- `MIGRATION_006` を no-op 化（sandbox_memory テーブルは 002 に canonical）
- `MIGRATION_008` を no-op 化（emotion_appraiser_md は 001/002 に canonical）
- migration 二重定義を検知する contract tests 追加（RED → GREEN 確認）
- prompt-bundle / workspace-draft / sandbox persistence contract tests 追加
- fresh DB smoke tests 追加

## Discoveries
- 2026-04-07: 前のサイクル（旧 PLAN.md）のテストが repo に残っており、plan_tdd.md のファイル名と一致していた。最初のラウンドでは「既存テストが通るから OK」と判断してしまい、実際の問題を修正せずスキップした。指摘を受けて T1 からやり直した。

## Decisions
- 2026-04-07: MIGRATION_004/006/008 は削除せず no-op (`SELECT 1`) にする。理由: `_migrations` テーブルで既に applied として記録されているため、変数名は残してトラッキングを維持する。
- 2026-04-07: MIGRATION_002 が sandbox テーブルの canonical 定義場所、MIGRATION_001 が prompt_bundle_versions の canonical 定義場所。

## Notes
- T2〜T9 は未着手（PLANS.md で Not started にリセット済み）
- 前回の評価で指摘された未解決問題:
  - `legacyComparison: null` 固定（T4/T5 のスコープ）
  - prod/draft trace persistence parity（T6 のスコープ）
- 400 テスト全パス、ci:local green
